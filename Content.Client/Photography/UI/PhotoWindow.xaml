<ui:PhotoWindow xmlns="https://spacestation14.io"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:viewport="clr-namespace:Content.Client.Viewport"
                      xmlns:ui="clr-namespace:Content.Client.Photography.UI"

                      MouseFilter="Stop"
                      Resizable="False">

    <BoxContainer Name="ContentsRoot" Orientation="Vertical">
        <PanelContainer StyleClasses="AngleRect" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="6">
            <TextureButton Name="CloseButton" StyleClasses="windowCloseButton"/>
        </PanelContainer>

        <PanelContainer Name="PhotoBackground" StyleClasses="PaperDefaultBorder" VerticalExpand="True" HorizontalExpand="True">
            <Control VerticalExpand="True" HorizontalExpand="True" Margin="5 5 5 5" Name="PhotoViewBox">
                <viewport:ScalingViewport Name="PhotoView"
                                        VerticalExpand="True"
                                        HorizontalExpand="True"
                                        MinSize="500 500"
                                        MouseFilter="Ignore" />
                <TextureRect VerticalExpand="True" HorizontalExpand="True" MinSize="500 500" Name="CameraViewBackground" />
            </Control>
        </PanelContainer>
    </BoxContainer>
</ui:PhotoWindow>
